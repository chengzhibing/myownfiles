1、Object.assign(target, originObj1, originObj2)
1)将原对象的值拷贝到目标对象，、
2）同属性（key），原对象的属性值会覆盖目标对象的属性值。后面的原对象属性值会覆盖前面的原对象的属性值。
3）如果第一层属性的属性值都是基本类型的话，目标对象修改对应的值不会改变原对象的同属性值。但是如果第一层
属性的属性值是引用类型的话，不论是目标对象还是原对象改变所引用的对象中的属性值，
对于原对象和目标对象会有影响。